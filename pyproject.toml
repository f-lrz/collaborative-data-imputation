[project]
name = "collaborative-data-imputation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "mlflow>=3.7.0",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "sortedcontainers>=2.4.0",
]
